{% extends 'base2.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>ContentC</th>
                <td>{{ commentaire.contentC }}</td>
            </tr>
            <tr>
                <th>PostedAt</th>
                <td>{{ commentaire.postedAt ? commentaire.postedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AuthorC</th>
                <td>{{ commentaire.authorC }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
