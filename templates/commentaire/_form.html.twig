<<<<<<< HEAD
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Article</title>

    <style>
        /* Style général du formulaire */
        form {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            background: linear-gradient(135deg, #f0f4f8, #e9ecef);
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            font-family: 'Poppins', sans-serif;
        }

        /* Style des champs de texte (input, textarea, select) */
        input, textarea, select {
            width: 100%;
            padding: 15px;
            margin: 12px 0;
            border: 2px solid #dee2e6;
            border-radius: 8px;
            font-size: 16px;
            transition: all 0.3s ease-in-out;
            background-color: #fff;
        }

        /* Effet au focus pour les champs de formulaire */
        input:focus, textarea:focus, select:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
            outline: none;
        }

        /* Style pour le bouton */
        button.btn {
            display: block;
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            font-size: 18px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background 0.3s ease-in-out, transform 0.3s ease-in-out;
        }

        /* Effet au survol du bouton */
        button.btn:hover {
            background: linear-gradient(135deg, #0056b3, #003d80);
            transform: translateY(-2px);
        }

        /* Style des labels */
        label {
            font-weight: bold;
            color: #343a40;
            display: block;
            margin-bottom: 8px;
        }

        /* Style du titre du formulaire */
        h1 {
            text-align: center;
            font-size: 2rem;
            color: #333;
            margin-bottom: 20px;
        }

        /* Ajouter un espacement au formulaire pour le rendre plus aéré */
        .form-container {
            padding: 20px;
            background-color: #f9f9f9;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        }
    </style>
</head>
<body>

    <div class="form-container">
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_widget(form) }}
            </div>
            <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>

</body>
</html>
=======
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
>>>>>>> 4f151ce4df8e3002be4787cbdb5ce73435b4aac6
